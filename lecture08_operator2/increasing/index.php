<?php

/**
 *  Increasing/Decreasing Operators.
 */

 $count = 0;

 $count = $count + 1;

 // ++$count 같은 경우에는 바로 계산을 하고 넘어가는식이다 
 // 마이너스가 같은 경우도 빼는것만 다르지 똑같다
 echo $count++; // -> 1
 echo $count; // -> 2
